<h1><span class="glyphicon glyphicon glyphicon-pencil" aria-hidden="true"></span> Post a new Algorithm</h1>

<div class="col-md-8">
	<form action="/posts" method="POST">
		<input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
		<div class="form-group">
			<label for="title">Title:</label>
			<em>What's a descriptive name for your algorithm?</em>
			<input type="text" class="form-control" name="post[title]" id="title">
		</div>
		<div class="form-group">
			<label for="code">Your Code:</label>
			<div id="editor-post" style="height: 250px"></div>
			<input type="hidden" name="post[code]" id="codearea" value="">	
			<script>
				var editor = ace.edit("editor-post");
				var code = editor.getSession().getValue();
				editor.setTheme("ace/theme/monokai");
				editor.getSession().setMode("ace/mode/javascript");
				editor.getSession().setTabSize(4);
				editor.setHighlightActiveLine(false);
				editor.setShowPrintMargin(false);
				editor.getSession().setUseWrapMode(true);

				editor.getSession().on('change', function(e) {
	    			code = editor.getSession().getValue();
				});

				$(document).ready(function() {
					$('#post-button').click(function() {
					  	$('#codearea').val(code);
				  	});
				});
			</script>
		</div>
		<div class="form-group">
			<label for="description">Description:</label>
			<em>What does your algorithm do?</em>
			<textarea class="form-control" rows="6" name="post[description]" id="description"></textarea>
		</div>
		<div class="form-group">
			<label for="difficulty">Difficulty Level:</label>
			<select class="form-control" name="post[difficulty]" id="difficulty">
				<option value="1">1</option>
				<option value="2">2</option>
				<option value="3">3</option>
				<option value="4">4</option>
				<option value="5">5</option>
			</select>
		</div>
		<div class="form-group">
			<label for="category">Category:</label>
			<select class="form-control" name="post[category]" id="category">
        <option value="Sort">Sort</option>
        <option value="Array">Array</option>
        <option value="LinkedList">LinkedList</option>
        <option value="Recursion">Recursion</option>
        <option value="BinaryTree">BinaryTree</option>
        <option value="Divide&Conquer">Divide&Conquer</option>
        <option value="Fibonacci">Fibonacci</option>
        <option value="Greedy">Greedy</option>
        <option value="Branch&Bound">Branch&Bound</option>
        <option value="BruteForce">BruteForce</option>
        <option value="Randomized">Randomized</option>
        <option value="Backtracking">Backtracking</option>
        <option value="Other">Other</option>
			</select>
		</div>
		<div class="pull-right">
			<a href="/posts" class="btn btn-danger">Cancel</a>
			<input type="submit" id="post-button" class="btn btn-primary" value="Post Algorithm" data-disable-with="Posting..">
		</div>
	</form>
</div>